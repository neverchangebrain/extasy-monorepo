{
  "name": "extasy-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:assistant": "bun --watch apps/assistant-bot/src/index.ts",
    "dev:close": "bun --watch apps/close-bot/src/index.ts",
    "dev:economy": "bun --watch apps/economy-bot/src/index.ts",
    "dev:event": "bun --watch apps/event-bot/src/index.ts",
    "dev:mod": "bun --watch apps/mod-bot/src/index.ts",
    "dev:staff": "bun --watch apps/staff-bot/src/index.ts",
    "format": "bunx prettier . --write",
    "infra:down": "docker compose -f docker-compose.local.yml down",
    "infra:logs": "docker compose -f docker-compose.local.yml logs -f",
    "infra:up": "docker compose -f docker-compose.local.yml up -d"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "@types/bun": "latest",
    "prettier": "^3.6.2",
    "prettier-plugin-organize-imports": "^4.3.0",
    "prettier-plugin-packagejson": "^3.0.2",
    "typescript": "^5.9.2"
  }
}
